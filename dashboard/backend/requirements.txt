# Lean dependency set for the API *service* only (used by the backend
# Dockerfile). The API serves precomputed CSV/JSON artifacts produced by
# pipelines/run_pipeline.py - it never runs pandas transforms beyond simple
# filtering, and never imports pymc/arviz/statsmodels - so it doesn't need
# the full data-science stack, which keeps the API container image small
# and avoids needing a C/C++ compiler in the runtime image.
#
# For local development (running the full pipeline, notebooks, tests), use
# the repo-root requirements.txt instead.
flask>=3.0,<4.0
flask-cors>=4.0,<5.0
python-dotenv>=1.0,<2.0
pandas>=2.1,<3.0
gunicorn>=22.0,<23.0
