server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unknown path serves index.html, letting client-side
    # routing (if added later) work behind a single nginx server block.
    location / {
        try_files $uri /index.html;
    }
}
